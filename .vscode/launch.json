{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run without Terminal",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "internalConsole",  // 关键设置：不显示终端
        }
    ]
    
}